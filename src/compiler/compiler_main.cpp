#include "yuan.h"
#include "utils.h"

int main(int argc, char **argv) 
{
    to_cwd(argv[0]);
    for (int i = 1; i < argc; ++i) {
        if (argv[i][0] != '-') {

        }
        else {
            
        }
    }
    return 0;
}